% load packages
\usepackage{geometry}
\usepackage{xcolor}
\usepackage{eso-pic}
\usepackage{fancyhdr}
\usepackage{sectsty}
\usepackage{fontspec}
\usepackage{titlesec}

%% Set page size with a wider right margin
%TS A4 paper is 210 x 297 mm. Letter is 215.9 x 279.4mm, so the difference is +5.9, -17.6mm
%%TS \geometry{a4paper, total={170mm,257mm}, left=20mm, top=20mm, bottom=20mm, right=50mm}
\geometry{letterpaper, total={175.9mm,239.4mm}, left=20mm, top=20mm, bottom=20mm, right=50mm}

%% Let's define some colours
%%TS \definecolor{light}{HTML}{E6E6FA}
%%TS dark is used for section headings
%%TS \definecolor{dark}{HTML}{330033}
\definecolor{highlight}{HTML}{800080}
\definecolor{light}{HTML}{ebebeb}
\definecolor{dark}{HTML}{1b9e77}

%% Let's add the border on the right hand side 
\AddToShipoutPicture{% 
    \AtPageLowerLeft{% 
        \put(\LenToUnit{\dimexpr\paperwidth-3cm},0){% 
            \color{light}\rule{3cm}{\LenToUnit\paperheight}%
          }%
     }%
     % logo
    \AtPageLowerLeft{% start the bar at the bottom right of the page
        %%TS \put(\LenToUnit{\dimexpr\paperwidth-2.25cm},27.2cm){% move it to the top right
        \put(\LenToUnit{\dimexpr\paperwidth-2.75cm},25.46cm){% move it to the top right
            \color{light}\includegraphics[width=2.5cm]{_extensions/tomslee/PrettyPDF/logo.png}
          }%
     }%
}

%% Style the page number
\fancypagestyle{mystyle}{
  \fancyhf{}
  \renewcommand\headrulewidth{0pt}
  \fancyfoot[R]{\thepage}
  \fancyfootoffset{3.5cm}
}
\setlength{\footskip}{20pt}

%% style the chapter/section fonts
\chapterfont{\color{dark}\fontsize{20}{16.8}\selectfont}
\sectionfont{\color{dark}\fontsize{20}{16.8}\selectfont}
\subsectionfont{\color{dark}\fontsize{14}{16.8}\selectfont}
\titleformat{\subsection}
  {\sffamily\Large\bfseries}{\thesection}{1em}{}[{\titlerule[0.8pt]}]
  
% left align title
\makeatletter
\renewcommand{\maketitle}{\bgroup\setlength{\parindent}{0pt}
\begin{center}
  {\sffamily\huge\textbf{\color{dark}\MakeUppercase{\@title}}} \vspace{0.3cm} \newline
  {\Large {\@subtitle}} \newline
  {\large\bfseries\@author \hfill \@date}
\end{center}\egroup
}
\makeatother

%% Use some custom fonts
\setsansfont{Ubuntu}[
    Path=_extensions/tomslee/PrettyPDF/Ubuntu/,
    Scale=0.9,
    Extension = .ttf,
    UprightFont=*-Regular,
    BoldFont=*-Bold,
    ItalicFont=*-Italic,
    ]

%%TS\setmainfont{Ubuntu}[
    %%TSPath=_extensions/tomslee/PrettyPDF/Ubuntu/,
    %%TSScale=0.9,
    %%TSExtension = .ttf,
    %%TSUprightFont=*-Regular,
    %%TSBoldFont=*-Bold,
    %%TSItalicFont=*-Italic,
    %%TS]
